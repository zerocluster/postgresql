dependencies:
  - "@softvisio/core"

cli:
  options:
    reset-root-password: ~

services:
  promary:
    compoents:
      postgresql:
        mode: primary
  standby-sync:
    compoents:
      postgresql:
        mode: standby-sync
  standby-async:
    compoents:
      postgresql:
        mode: standby-async
  standby-backup:
    compoents:
      postgresql:
        mode: standby-backup

defaultService: primary

components: ~

config: ~
